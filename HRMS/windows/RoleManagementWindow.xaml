<dx:ThemedWindow
    x:Class="HRMS.windows.RoleManagementWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    Title="{DynamicResource roleManagementWindow}" Width="620" Height="480"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="rolesGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource mysqlAccount}" Binding="{Binding Username}" Width="*" />
                <DataGridTextColumn Header="{DynamicResource role}" Binding="{Binding Role}" Width="150" />
                <DataGridCheckBoxColumn Header="{DynamicResource active}" Binding="{Binding IsActive}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" /><ColumnDefinition Width="170" /><ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="usernameTextBox" Height="32" Margin="0,0,8,0" />
            <ComboBox x:Name="roleComboBox" Grid.Column="1" Height="32" Margin="0,0,8,0" />
            <CheckBox x:Name="activeCheckBox" Grid.Column="2" Content="{DynamicResource active}" IsChecked="True" VerticalAlignment="Center" />
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="{DynamicResource saveRole}" Width="130" Height="34" Click="save_Click" />
            <TextBlock x:Name="statusText" Margin="14,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</dx:ThemedWindow>
